import Logout from "@/components/Logout";

export const metadata = {
  title: "Logout - D-Libro | Digital Book Library for Web Skill Training",
};

const LogoutPage = () => {
  return <Logout />;
};

export default LogoutPage;
